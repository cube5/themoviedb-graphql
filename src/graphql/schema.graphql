# import { Discover } from "discover/discover.graphql"
# import { Search } from "search/search.graphql"
# import { Movie } from "movie/movie.graphql"

"""
Schema definition.

This file contains the Queries and Mutations implemented so far,
there should not be other files containing this kind of operations.
"""
type Query {
  discover: Discover
  search: Search
  movie(id: ID!): Movie
}

schema {
  query: Query
}
